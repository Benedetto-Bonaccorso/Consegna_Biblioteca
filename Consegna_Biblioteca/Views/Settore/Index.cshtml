@model IEnumerable<Consegna_Biblioteca.Models.Settore>

@{
    ViewBag.Title = "Index";
}

<h2>Indice Settori</h2>

<p>
    @Html.ActionLink("Crea", "Create","", new { @class = "LinkAzione" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Modifica", "Edit", new { id=item.Id_Settore }, new { @class = "LinkAzione" })
        </td>
    </tr>
}

</table>
